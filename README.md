# musica-radiation-transfer

To build the library and tests:

```
 mkdir build
 cd build
 cmake [-DCMAKE_BUILD_TYPE=Debug] ..
 make
```

To run the tests from the `build/` directory:

```
 make test
```
